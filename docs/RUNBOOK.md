# Runbook

Placeholder — this becomes real in Phase 6 (Hardening & go-live), once
there's a production system to operate. It will cover: what to do when a
Wolt delivery creation fails repeatedly, what to do when Stripe capture
fails, how to read the `jobs` dead-letter queue, how to restart the job
worker, how to run a database restore drill, and who to call (Wolt
technical contact, Stripe support, hosting support) for each category of
incident.

Kept here as a stub so the intent is visible from Phase 0 onward, per
`docs/ARCHITECTURE.md` Section L.7.
